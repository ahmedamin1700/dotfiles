# General Settings
backend = "glx";

# Blur Settings
blur: {
  method = "dual_kawase";
  strength = 7;
  background = true;
};

# Only blur specific windows like Alacritty
blur-background-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'",
  "_GTK_FRAME_EXTENTS@:c"
];

